#include "monty.h"

/**
 * f_stack - Switches the stack mode.
 * @head: Stack head (unused)
 * @counter: Line number (unused)
 * Return: No return
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
